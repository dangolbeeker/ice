interface IParams {
    rootDir: string;
    tempDir: string;
    configPath: string;
    projectType: string;
    isMpa: boolean;
    srcDir: string;
}
interface IResult {
    routesPath: string;
    isConfigRoutes: boolean;
}
declare function getRoutes({ rootDir, tempDir, configPath, projectType, isMpa, srcDir }: IParams): IResult;
export default getRoutes;
