"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var path = require("path");
var fse = require("fs-extra");
function getPages(rootDir, srcDir) {
    var pagesPath = path.join(rootDir, srcDir, 'pages');
    return fse.existsSync(pagesPath) ? fse.readdirSync(pagesPath)
        .filter(function (page) {
        // filter .xxx and _xxx
        return !/^[._]/.test(page);
    })
        .map(function (page) {
        var name = path.parse(page).name;
        return name;
    }) : [];
}
exports.default = getPages;
