import { History, Location } from 'history';
declare let history: History;
declare function createHistory({ routes, customHistory, type, basename, location }: {
    routes?: any[];
    customHistory?: History;
    type?: string;
    basename?: string;
    location?: Location;
}): History<unknown>;
declare function getHistory(): History<unknown>;
export { getHistory, createHistory, history };
