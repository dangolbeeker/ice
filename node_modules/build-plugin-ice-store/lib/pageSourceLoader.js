"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
Object.defineProperty(exports, "__esModule", { value: true });
var loader_utils_1 = require("loader-utils");
var path_1 = require("path");
/**
 * Amend page source in app.json routes field
 * return {
 *  "routes": [
      {
        "path": "/",
        "source": ".rax/pages/Home/Home",
      }
    ]
  }
 */
function pageSourceLoader(appJSON) {
    var targetPath = loader_utils_1.getOptions(this).targetPath;
    var content = JSON.parse(appJSON);
    content.routes = content.routes.map(function (route) {
        var pageSource = route.source;
        if (/^\/?pages/.test(pageSource)) {
            if (/index$/.test(pageSource)) {
                pageSource = pageSource.replace(/index$/, 'Page');
            }
            else {
                pageSource = path_1.join(pageSource, 'Page');
            }
            return __assign(__assign({}, route), { pageSource: path_1.join(targetPath, pageSource) });
        }
        return route;
    });
    return JSON.stringify(content);
}
exports.default = pageSourceLoader;
