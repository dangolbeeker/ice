/**
 * Amend page source in app.json routes field
 * return {
 *  "routes": [
      {
        "path": "/",
        "source": ".rax/pages/Home/Home",
      }
    ]
  }
 */
export default function pageSourceLoader(appJSON: any): string;
