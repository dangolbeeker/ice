export interface IRenderPageParams {
    pageName: string;
    pageNameDir: string;
    pageModelsDir: string;
    pageModelFile: string;
    pageStoreFile: string;
    existedStoreFile: boolean;
}
export default class Generator {
    private rootDir;
    private appStoreTemplatePath;
    private pageStoreTemplatePath;
    private typesTemplatePath;
    private targetPath;
    private projectType;
    private applyMethod;
    private srcDir;
    private resetPageState;
    constructor({ rootDir, appStoreTemplatePath, pageStoreTemplatePath, typesTemplatePath, targetPath, applyMethod, projectType, srcDir, resetPageState }: {
        rootDir: string;
        appStoreTemplatePath: string;
        pageStoreTemplatePath: string;
        pageStoresTemplatePath: string;
        typesTemplatePath: string;
        targetPath: string;
        projectType: string;
        applyMethod: Function;
        srcDir: string;
        resetPageState: boolean;
    });
    private getPageModels;
    private renderAppStore;
    private renderAppStoreTypes;
    private renderPageStore;
    private renderPageComponent;
    private renderPageLayout;
    private renderPageIndex;
    render(): void;
}
